@model List<NationalityModel>
@{
    ViewData["Title"] = "Nationality | HeroHRM human resource manager";
}
<partial name="_adminPartial" />

<section class="container mt-3">
    <div class="card">
        <div class="card-header text-center">
            <h3>Nationality</h3>
        </div>
        <div class="card-body">
            <a class="btn btn-success" asp-controller="Nationality" asp-action="CreateNationality">
                Add Nationality
            </a>
        </div>
    </div>
    <div>
        @if (Model.Count > 0)
        {

            <table class="table table-striped table-hover border">
                <thead class="table-info">
                    <tr>
                        <th>
                            <label>Name</label>
                        </th>
                        <th>
                            <label>Created Date</label>
                        </th>
                        <th>
                            <label>Updated Date</label>
                        </th>
                        <th>
                            <label>Action</label>
                        </th>
                    </tr>

                </thead>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(m => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(m => item.CreatedDate)
                        </td>
                        <td>
                            @Html.DisplayFor(m => item.UpdatedDate)
                        </td>

                        <td>
                            <a class="btn btn-outline-info" asp-controller="Nationality" asp-action="EditNationality" asp-route-Id="@item.Id">Edit</a>

                            <a class="btn btn-outline-danger" asp-controller="Nationality" asp-action="DeleteNationality" asp-route-Id="@item.Id">Delete</a>

                        </td>
                    </tr>
                }

            </table>

        }
        else
        {
            <h3>No Information Found</h3>
        }


    </div>
</section>